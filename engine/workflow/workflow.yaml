# current.yaml — generated by init; DIRECTION phase reads+overwrites project: and dials
project:
  name: # filled by init + DIRECTION phase per project
  mode: null        # set by DIRECTION (persuade | operate | read | experience)
  audience: null
stack: defaults
design:
  variance: null    # 1-10, dials get filled by the Design Read
  motion_intensity: null
  density: null
  radius: sharp     # default
  accent: 1
bans: 
  - no ai-purple gradients
  - no Inter-as-default
  - no Fraunces/Instrument Serif
  - min-h-[100dvh] not h-screen
  - no ease-in on UI
  - no scale(0)
  - eyebrow on max 1-in-3 sections
  - no duplicate CTA intent
skills: 
  - intake
  - direction
  - build
  - motion
  - verify
  - polish
